import Button from "./Button";

const POSButtons = () => {
  return (
    <div className="buttons-container">
      <Button className="big-button">ADD</Button>
      <Button className="big-button">Remove</Button>
      <Button className="big-button">Discount</Button>
    </div>
  );
};

export default POSButtons;
